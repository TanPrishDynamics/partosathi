# ─── e-Partogram Backend Dependencies ────────────────────────────────────────
# All versions pinned for reproducible, secure production builds.
# To install: pip install -r requirements.txt

# ── Core Web Framework ────────────────────────────────────────────────────────
Flask==3.1.0
Flask-Cors==5.0.0
Werkzeug==3.1.3

# ── Database ORM ──────────────────────────────────────────────────────────────
Flask-SQLAlchemy==3.1.1
SQLAlchemy==2.0.49
PyMySQL==1.1.1           # MySQL driver (for production migration)

# ── Authentication & Security ─────────────────────────────────────────────────
Flask-JWT-Extended==4.7.1
Flask-Limiter==4.1.1     # Rate limiting (brute-force protection)
flask-talisman==1.1.0    # HTTP security headers (CSP, HSTS, X-Frame-Options)

# ── Input Validation ──────────────────────────────────────────────────────────
marshmallow==3.26.1      # Schema-based input validation and sanitization

# ── Export ───────────────────────────────────────────────────────────────────
reportlab==4.2.5         # PDF generation for partogram export

# ── ML / AI (unpinned — large packages, pin via pip freeze for prod) ──────────
torch>=2.0.0
pandas>=2.0.0
numpy>=1.24.0
scikit-learn>=1.3.0
