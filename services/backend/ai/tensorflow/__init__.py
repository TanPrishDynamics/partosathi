"""
services/backend/ai/tensorflow/__init__.py

TensorFlow/Keras production LSTM pipeline.

Modules:
    model          — LSTM architecture definition (Keras Sequential)
    preprocessing  — Feature engineering & scaler pipeline
    predict        — LaborPredictor class for production inference
    train          — Model training & evaluation pipeline
    validation     — Input validation for temporal sequence data
    streamlit_app  — Interactive model monitoring dashboard
"""
