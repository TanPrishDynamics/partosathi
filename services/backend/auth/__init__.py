"""
services/backend/auth/__init__.py

Authentication and security utilities for e-Partogram.

Modules:
    crypto   — Password hashing, PHI field encryption (Fernet), IDOR helpers
    lockout  — Account lockout logic (brute-force protection)
    mfa      — TOTP multi-factor authentication (pyotp)
"""
